# Migration `20201107153045-cast-model`

This migration has been generated by ruheni at 11/7/2020, 6:30:45 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "Actor" (
    "id" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "alsoKnownAs" TEXT NOT NULL,
    "movieId" TEXT,

    FOREIGN KEY ("movieId") REFERENCES "Movie"("id") ON DELETE SET NULL ON UPDATE CASCADE,
    PRIMARY KEY ("id")
)

PRAGMA foreign_keys=OFF;
CREATE TABLE "new_Movie" (
    "id" TEXT NOT NULL,
    "title" TEXT NOT NULL,
    "description" TEXT NOT NULL,
    "coverImage" TEXT NOT NULL,
    "rating" INTEGER NOT NULL,
    "releaseDate" DATETIME NOT NULL,
    PRIMARY KEY ("id")
);
INSERT INTO "new_Movie" ("id", "title", "description", "coverImage") SELECT "id", "title", "description", "coverImage" FROM "Movie";
DROP TABLE "Movie";
ALTER TABLE "new_Movie" RENAME TO "Movie";
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201107132501-delete-rating..20201107153045-cast-model
--- datamodel.dml
+++ datamodel.dml
@@ -1,15 +1,26 @@
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
 }
 model Movie {
-  id          String @id @default(uuid())
+  id          String   @id @default(uuid())
   title       String
   description String
   coverImage  String
+  rating      Int
+  releaseDate DateTime
+  cast        Actor[]
 }
+
+model Actor {
+  id          String  @id @default(uuid())
+  name        String
+  alsoKnownAs String
+  Movie       Movie?  @relation(fields: [movieId], references: [id])
+  movieId     String?
+}
```


